# playground-ai

A playground for AI experiments and learning.

## Cloud GPU Setup

As `root`:

```bash
curl -fsSL https://raw.githubusercontent.com/giacomoran/playground-ai/main/setup-ssh-root.sh | bash
```

As `giacomo`:

```bash
curl -fsSL https://raw.githubusercontent.com/giacomoran/playground-ai/main/2025-10-simclr/setup-ssh.sh | bash
```

(Replace `2025-10-simclr` with your project directory if different)
